import React from "react";
import { MarginContainer } from "../../styled-components/view";

const Article: React.FC = (): JSX.Element => (
	<MarginContainer>
		<h1>Article</h1>
	</MarginContainer>
);

export default Article;
