import React from "react";
import { CoursesSection } from "../styledComponents/views";

const Courses: React.FC = (): JSX.Element => {
	const data = "sdfsdf";
	return (
		<CoursesSection>
			<h1>{data}</h1>
		</CoursesSection>
	);
};

export default Courses;
